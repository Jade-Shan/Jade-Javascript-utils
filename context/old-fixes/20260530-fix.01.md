# UIWindow.ts 代码审查报告

审查日期：2026/05/30

---

## 一、Bug 类问题

### 1. DockBar.removeIcon 中存在死代码路径（逻辑错误）

**位置**：第 871 行 & 第 876 行

**问题**：外层 `if (items.length > 0)` 保证了进入循环时 `items.length > 0`，但内部第 876 行又检查 `if (items.length == 0)`，这个条件永远为 false，是死代码。

**修改前**：
```typescript
removeIcon(win: UIObj) {
    let iconId = this.genAppIconId(win.id);
    let items = this.barDiv.children;
    if (items.length > 0) {
        for (let i = 0; i < items.length; i++) {
            if (iconId === items.item(i)?.id) {
                let gap : Element | null = null;
                let icon: Element | null = items.item(i);
                if (items.length == 0) {
                    // 最后一个，没有间隔符  ← 永远不会执行
                } else if (i == 0 && items.item(i + 1)?.classList.contains("gap")) {
                    gap = items.item(i + 1);
                } else if (items.item(i - 1)?.classList.contains("gap")) {
                    gap = items.item(i - 1);
                }
                if (icon) { this.barDiv.removeChild(icon); }
                if (gap ) { this.barDiv.removeChild(gap ); }
            }
        }
    }
}
```

**修改后**：
```typescript
removeIcon(win: UIObj) {
    let iconId = this.genAppIconId(win.id);
    let items = this.barDiv.children;
    for (let i = 0; i < items.length; i++) {
        if (iconId === items.item(i)?.id) {
            let gap : Element | null = null;
            let icon: Element | null = items.item(i);
            if (items.length == 1) {
                // 最后一个元素，没有间隔符
            } else if (i == 0 && items.item(i + 1)?.classList.contains("gap")) {
                gap = items.item(i + 1);
            } else if (items.item(i - 1)?.classList.contains("gap")) {
                gap = items.item(i - 1);
            }
            if (icon) { this.barDiv.removeChild(icon); }
            if (gap ) { this.barDiv.removeChild(gap ); }
            break; // 找到后退出，避免删除后索引错乱
        }
    }
}
```

**说明**：
- 移除了不可达的外层 `if (items.length > 0)` 包装
- 将 `items.length == 0` 修正为 `items.length == 1`（当前只剩最后一个图标时不删间隔符）
- 添加 `break`：删除 DOM 元素后继续遍历 `children` 会导致索引错乱

---

### 2. XSS 风险：使用 innerHTML 设置标题文本

**位置**：第 919 行

**问题**：`titleBarText.innerHTML = win.title` 如果 `win.title` 包含用户输入的 HTML，会导致 XSS 注入。

**修改前**：
```typescript
titleBarText.innerHTML = win.title;
```

**修改后**：
```typescript
titleBarText.textContent = win.title;
```

---

### 3. 缩放时 bodyHeight 计算使用了旧的 offsetHeight

**位置**：第 498 行

**问题**：在 `bindWindowScaleMoving` 的 `mousemove` 中，计算 `bodyHeight` 使用 `winDiv.offsetHeight`（DOM 的当前值），但紧接着才设置 `winDiv.style.height`。这意味着 bodyHeight 是基于上一帧的高度计算的，应使用 `end.height`。

**修改前**：
```typescript
let bodyHeight = winDiv.offsetHeight - currWin.ui.titleBar.clientHeight;
if (currWin.ui.statusBar) {
    bodyHeight = bodyHeight - currWin.ui.statusBar.clientHeight;
}
//
winDiv.style.left   = `${end.left  }px`;
winDiv.style.top    = `${end.top   }px`;
winDiv.style.width  = `${end.width }px`;
winDiv.style.height = `${end.height}px`;
currWin.ui.windowBody.style.height = `${bodyHeight - 25}px`;
currWin.ui.windowBody.style.width  = `${end.width  - 25}px`;
```

**修改后**：
```typescript
winDiv.style.left   = `${end.left  }px`;
winDiv.style.top    = `${end.top   }px`;
winDiv.style.width  = `${end.width }px`;
winDiv.style.height = `${end.height}px`;
let bodyHeight = end.height - currWin.ui.titleBar.clientHeight;
if (currWin.ui.statusBar) {
    bodyHeight = bodyHeight - currWin.ui.statusBar.clientHeight;
}
currWin.ui.windowBody.style.height = `${bodyHeight - 25}px`;
currWin.ui.windowBody.style.width  = `${end.width  - 25}px`;
```

---

## 二、代码质量 / 可维护性问题

### 4. 变量遮蔽（Variable Shadowing）

**位置**：第 394 行 & 第 424 行

**问题**：`bindWindowScaleSelect` 中外部定义了 `let desktop = win.desktop;`（第 394 行），内部 mousedown 回调又重复声明了 `let desktop = win.desktop;`（第 424 行），造成变量遮蔽。

**修改前**：
```typescript
bindWindowScaleSelect: (win: UIObj): any=> {
    let desktop = win.desktop;         // ← 第一次声明
    let winDiv = win.ui.win;
    // ...
    winDiv.addEventListener("mousedown", (e) => {
        if (win.cfg.scalable && !win.status.isMax && !win.status.isMin) {
            let desktop = win.desktop; // ← 遮蔽了外层的 desktop
            let desktopDiv = desktop.desktopDiv;
            // ...
```

**修改后**：
```typescript
bindWindowScaleSelect: (win: UIObj): any=> {
    let desktop = win.desktop;
    let winDiv = win.ui.win;
    // ...
    winDiv.addEventListener("mousedown", (e) => {
        if (win.cfg.scalable && !win.status.isMax && !win.status.isMin) {
            let desktopDiv = desktop.desktopDiv; // 直接使用外层的 desktop
            // ...
```

---

### 5. `checkScaleStart` 函数参数冗余

**位置**：第 396 行

**问题**：`checkScaleStart` 接收 `winDiv` 参数，但它定义在已经能通过闭包访问同名 `winDiv` 的作用域内，参数与闭包变量指向同一对象。

**修改前**：
```typescript
let checkScaleStart = (winDiv: HTMLElement, e: MouseEvent): 1|2|3|4|5|6|7|8|9 => {
    // ...
};
// 调用
let direction = checkScaleStart(winDiv, e);
```

**修改后**：
```typescript
let checkScaleStart = (e: MouseEvent): 1|2|3|4|5|6|7|8|9 => {
    // 直接使用闭包中的 winDiv
    // ...
};
// 调用
let direction = checkScaleStart(e);
```

---

### 6. 类型不安全的 `any` 转换

**位置**：第 833 行

**问题**：`let elm: any = item;` 将 `Element` 强转为 `any` 来访问 style 属性，丢失了类型检查。

**修改前**：
```typescript
let item: Element | null = items.item(i);
if (item != null) {
    let elm: any = item;
    let rect = item.getBoundingClientRect();
    let x = rect.left + rect.width / 2;
    let scale = sizeCurve(x);
    elm.style.setProperty('--i', `${scale}`);
    let opacity = opacityCurve(x)
    elm.style.opacity = `${opacity}%`;
}
```

**修改后**：
```typescript
let item = items.item(i) as HTMLElement | null;
if (item != null) {
    let rect = item.getBoundingClientRect();
    let x = rect.left + rect.width / 2;
    let scale = sizeCurve(x);
    item.style.setProperty('--i', `${scale}`);
    let opacity = opacityCurve(x);
    item.style.opacity = `${opacity}%`;
}
```

---

### 7. 魔法数字应提取为常量

**位置**：多处（第 336、339、351、352、495、507、508 行）

**问题**：`-6`（边框补偿）、`-25`（body 内边距补偿）、`-10`（dock 留白）反复出现，含义不明且修改时容易遗漏。

**修改前**：
```typescript
end.width  = pElem.clientWidth - 6;
end.height = win.desktop.hasDockBar() ?
        pElem.clientHeight - 10 : pElem.clientHeight - 6;
// ...
win.ui.windowBody.style.height = `${bodyHeight - 25}px`;
win.ui.windowBody.style.width  = `${end.width  - 25}px`;
```

**修改后**：
```typescript
const WIN_BORDER_SIZE = 6;
const WIN_BODY_PADDING = 25;
const DOCK_BAR_MARGIN = 10;
// ...
end.width  = pElem.clientWidth - WIN_BORDER_SIZE;
end.height = win.desktop.hasDockBar() ?
        pElem.clientHeight - DOCK_BAR_MARGIN : pElem.clientHeight - WIN_BORDER_SIZE;
// ...
win.ui.windowBody.style.height = `${bodyHeight - WIN_BODY_PADDING}px`;
win.ui.windowBody.style.width  = `${end.width  - WIN_BODY_PADDING}px`;
```

---

### 8. `hasDockBar()` 可简化

**位置**：第 92 行

**修改前**：
```typescript
hasDockBar(): boolean { return this.dockBar ? true : false; }
```

**修改后**：
```typescript
hasDockBar(): boolean { return this.dockBar !== undefined; }
```

---

### 9. `genWinId` 使用 `new Date().getTime()` 可能生成重复 ID

**位置**：第 897 行

**问题**：如果在同一毫秒内创建多个同 id 前缀的窗口，会生成相同的 ID。

**修改前**：
```typescript
export function genWinId(id: string): string { return `${id}-${(new Date()).getTime()}`; }
```

**修改后**：
```typescript
let winIdCounter = 0;
export function genWinId(id: string): string { return `${id}-${Date.now()}-${winIdCounter++}`; }
```

---

### 10. `optWinClose` 与 `optWinActive` 重复逻辑可提取

**位置**：第 165-212 行

**问题**：两个方法都有"过滤指定窗口 → 重建 z-index 数组 → 重新赋值层级"的相同模式。

**修改前**：
```typescript
optWinClose(win: UIObj) {
    this.allWindows.remove(win.id);
    let newIndex: Array<UIObj> = [];
    for (let i = 0; i < this.windowZIndex.length; i++) {
        let w = this.windowZIndex[i];
        if (win.id === w.id) { } else {
            w.activeWindow(false);
            w.setZIndex(WIN_Z_IDX_MIN + newIndex.length);
            newIndex.push(w);
        }
    }
    this.windowZIndex = newIndex;
    // ...
}

optWinActive(win: UIObj) {
    let newIndex: Array<UIObj> = [];
    for (let i = 0; i < this.windowZIndex.length; i++) {
        let w = this.windowZIndex[i];
        if (w.id === win.id) { } else {
            w.activeWindow(false);
            w.setZIndex(WIN_Z_IDX_MIN + newIndex.length);
            newIndex.push(w);
        }
    }
    newIndex.push(win);
    this.windowZIndex = newIndex;
    // ...
}
```

**修改后**：
```typescript
private reorderWindows(excludeId: string): Array<UIObj> {
    let newIndex: Array<UIObj> = [];
    for (let i = 0; i < this.windowZIndex.length; i++) {
        let w = this.windowZIndex[i];
        if (w.id !== excludeId) {
            w.activeWindow(false);
            w.setZIndex(WIN_Z_IDX_MIN + newIndex.length);
            newIndex.push(w);
        }
    }
    return newIndex;
}

optWinClose(win: UIObj) {
    this.allWindows.remove(win.id);
    let newIndex = this.reorderWindows(win.id);
    this.windowZIndex = newIndex;
    this.desktopDiv.removeChild(win.ui.win);
    if (newIndex.length > 0) {
        let currWin = newIndex[newIndex.length - 1];
        currWin.activeWindow(true);
        currWin.setZIndex(WIN_Z_IDX_MIN + newIndex.length);
    }
    if (this.dockBar) { this.dockBar.removeIcon(win); }
}

optWinActive(win: UIObj) {
    let newIndex = this.reorderWindows(win.id);
    newIndex.push(win);
    this.windowZIndex = newIndex;
    win.activeWindow(true);
    win.setZIndex(WIN_Z_IDX_MIN + newIndex.length);
}
```

---

### 11. 构造函数中配置合并逻辑冗长

**位置**：第 683-691 行（UIWindowAdapter）、第 775-782 行（DockBar）

**问题**：逐字段 if 判断赋值，可用对象展开或 `Object.assign` 简化。

**修改前**：
```typescript
if (cfg) {
    if (cfg.bindWinOpt) { this.cfg.bindWinOpt = cfg.bindWinOpt; }
    if (cfg.icons) { this.cfg.icons = cfg.icons; }
    if (cfg.scalable != undefined) { this.cfg.scalable = cfg.scalable; }
    if (cfg.body) { 
        if (cfg.body.overflow) { this.cfg.body.overflow = cfg.body.overflow; }
        if (cfg.body.initSize) { this.cfg.body.initSize = cfg.body.initSize; }
    }
}
```

**修改后**：
```typescript
if (cfg) {
    if (cfg.bindWinOpt) { this.cfg.bindWinOpt = cfg.bindWinOpt; }
    if (cfg.icons) { this.cfg.icons = cfg.icons; }
    if (cfg.scalable !== undefined) { this.cfg.scalable = cfg.scalable; }
    if (cfg.body) {
        this.cfg.body = { ...this.cfg.body, ...cfg.body };
    }
}
```

**说明**：对于 `body` 这种纯数据子对象，展开运算符更简洁且不会遗漏新增字段。另外 `!= undefined` 改为 `!== undefined` 避免隐式类型转换。

---

### 12. `let` 应改为 `const`（多处）

**位置**：第 108、109、116、138 行等

**问题**：许多只赋值一次的变量使用了 `let`，应使用 `const` 表达不变意图。

**修改前**：
```typescript
let margin = 90;
let maxLevel = 8;
let maxWidth = this.desktopDiv.clientWidth - margin;
let maxHeigh = this.desktopDiv.clientHeight - margin;
```

**修改后**：
```typescript
const margin = 90;
const maxLevel = 8;
const maxWidth = this.desktopDiv.clientWidth - margin;
const maxHeight = this.desktopDiv.clientHeight - margin;
```

**附注**：`maxHeigh` 也是一个拼写错误，应为 `maxHeight`。

---

### 13. 方法闭括号后多余分号

**位置**：第 48、53 行

**修改前**：
```typescript
getCurrDragging() { return this.currWin.dragging;};
getCurrScaling() { return this.currWin.scaling;};
```

**修改后**：
```typescript
getCurrDragging() { return this.currWin.dragging; }
getCurrScaling() { return this.currWin.scaling; }
```

---

## 三、问题汇总

| # | 类型 | 严重度 | 位置 | 简述 |
|---|------|--------|------|------|
| 1 | Bug | 高 | L871-876 | removeIcon 死代码 + 遍历中删除导致索引错乱 |
| 2 | 安全 | 高 | L919 | innerHTML XSS 风险 |
| 3 | Bug | 中 | L498 | 缩放时 bodyHeight 基于旧帧计算 |
| 4 | 质量 | 低 | L394/424 | 变量遮蔽 |
| 5 | 质量 | 低 | L396 | 冗余函数参数 |
| 6 | 质量 | 中 | L833 | any 类型转换 |
| 7 | 可维护 | 中 | 多处 | 魔法数字 |
| 8 | 质量 | 低 | L92 | 冗余三元表达式 |
| 9 | Bug | 低 | L897 | ID 可能重复 |
| 10 | 可维护 | 中 | L165-212 | 重复逻辑可提取 |
| 11 | 质量 | 低 | L683-691 | 配置合并冗长 |
| 12 | 质量 | 低 | 多处 | let 应为 const + 拼写错误 |
| 13 | 质量 | 低 | L48/53 | 多余分号 |

---

## 四、优先修复建议

1. **立即修复**：#1（removeIcon 逻辑错误）、#2（XSS）、#3（bodyHeight 计算错误）
2. **建议修复**：#7（魔法数字）、#10（重复逻辑）、#6（类型安全）
3. **可选优化**：#4、#5、#8、#9、#11、#12、#13


# sandtable.ts 代码审查报告

审查日期：2026/05/30

---

## 问题 1：拼写错误 `visiable` → `visible`

**严重程度**：低（一致性/可读性问题）

整个文件中 `visible` 被拼写为 `visiable`，涉及接口定义和类属性。

**问题代码**：
```typescript
export interface IToken2DRec {
    // ...
    visiable : boolean,
    // ...
}
export interface IToken2D extends CanvasShape2D {
    // ...
    visiable : boolean,
    // ...
}
export class CircleToken extends CanvasCircle2D implements IToken2D, ICircleToken {
    visiable : boolean = true;
    // ...
}
```

**修改方案**：
```typescript
export interface IToken2DRec {
    // ...
    visible : boolean,
    // ...
}
export interface IToken2D extends CanvasShape2D {
    // ...
    visible : boolean,
    // ...
}
export class CircleToken extends CanvasCircle2D implements IToken2D, ICircleToken {
    visible : boolean = true;
    // ...
}
```

> 注意：此修改影响序列化数据结构（IToken2DRec），若已有持久化数据需要做兼容处理。

---

## 问题 2：`SandTable.observer` 的 `viewRange` 类型签名与 `IObserver` 不一致

**严重程度**：中（类型安全问题）

`IObserver` 接口定义 `viewRange` 参数类型为 `VisibilityType`，但 `SandTable` 类中的初始化使用了 `(type: string)`。

**问题代码**（第 314-318 行）：
```typescript
export class SandTable implements ISandTable {
    scene: IScene;
    observer: IObserver = {
        c: { x: 250, y: 300 },
        viewRange: (type: string) => { return 350; }
    };
```

**修改方案**：
```typescript
export class SandTable implements ISandTable {
    scene: IScene;
    observer: IObserver = {
        c: { x: 250, y: 300 },
        viewRange: (type: VisibilityType) => { return 350; }
    };
```

---

## 问题 3：`ISandTable` 接口为空，缺乏实际契约约束

**严重程度**：低（设计问题）

`ISandTable` 接口（第 311-312 行）没有定义任何成员，不能为使用者提供类型约束。

**问题代码**：
```typescript
export interface ISandTable {
}
```

**修改方案**：
```typescript
export interface ISandTable {
    scene: IScene;
    observer: IObserver;
    drawSceneWithUserView(proxyCfg?: ImageProxyConfig): Promise<void>;
}
```

---

## 问题 4：`ScenceDataResp` 拼写错误 → `SceneDataResp`

**严重程度**：低（命名一致性）

**问题代码**（第 288 行）：
```typescript
export interface ScenceDataResp {
```

**修改方案**：
```typescript
export interface SceneDataResp {
```

---

## 问题 5：`fromRecord` 方法中冗余的 null 检查和 for 循环可简化

**严重程度**：低（代码风格/可读性）

`CircleToken.fromRecord` 和 `RectangleToken.fromRecord` 中使用了传统 for 循环加 null 判断，可以用 `find` 简化。

**问题代码**（第 72-86 行）：
```typescript
static fromRecord(rec: ICircleTokenRec, imageRecs: Array<ImageResource>): CircleToken {
    let cc = new CircleToken(rec.id, rec.x, rec.y, rec.radius, 0, rec.color, rec.color,
        rec.visiable, rec.blockView, rec.color, rec.img);
    if (null != imageRecs && imageRecs.length > 0) {
        for (let i = 0; i < imageRecs.length; i++) {
            let img = imageRecs[i];
            if (img.id == rec.img.imgKey) {
                if (img.imgElem) {
                    rec.img.imageElem = img.imgElem;
                }
            }
        }
    }
    return cc;
}
```

**修改方案**：
```typescript
static fromRecord(rec: ICircleTokenRec, imageRecs: Array<ImageResource>): CircleToken {
    let cc = new CircleToken(rec.id, rec.x, rec.y, rec.radius, 0, rec.color, rec.color,
        rec.visible, rec.blockView, rec.color, rec.img);
    const matched = imageRecs?.find(img => img.id === rec.img.imgKey);
    if (matched?.imgElem) {
        rec.img.imageElem = matched.imgElem;
    }
    return cc;
}
```

---

## 问题 6：`fromRecord` 方法直接修改传入的 `rec` 参数（副作用）

**严重程度**：中（隐藏的副作用）

`CircleToken.fromRecord` 和 `RectangleToken.fromRecord` 都会修改传入的 `rec.img.imageElem`。调用者可能不期望 `rec` 被修改。

**问题代码**：
```typescript
rec.img.imageElem = img.imgElem;  // 修改了入参 rec
```

**修改方案**：
```typescript
static fromRecord(rec: ICircleTokenRec, imageRecs: Array<ImageResource>): CircleToken {
    const imgClip = { ...rec.img };
    const matched = imageRecs?.find(img => img.id === rec.img.imgKey);
    if (matched?.imgElem) {
        imgClip.imageElem = matched.imgElem;
    }
    let cc = new CircleToken(rec.id, rec.x, rec.y, rec.radius, 0, rec.color, rec.color,
        rec.visible, rec.blockView, rec.color, imgClip);
    return cc;
}
```

---

## 问题 7：`drawBrightScene` 中硬编码的 `sleep(1000)` 调用

**严重程度**：中（性能/逻辑问题）

第 470-472 行的 `drawItems` 回调只是 sleep 1 秒，这在实际使用中应该是绘制 token 的逻辑，但当前传入的是空操作加延迟。

**问题代码**（第 470-472 行）：
```typescript
let brightMapImage = await drawBrightScene(scene.frame.buff, oriMap, async (frame) => {
    await TimeUtil.sleep(1000); 
});
```

**修改方案**：
```typescript
let brightMapImage = await drawBrightScene(scene.frame.buff, oriMap, async (frame) => {
    // 在此绘制场景中的 token（teams, furnishings, walls 等）
});
```

> 如果确实需要等待某些异步资源加载完毕，应使用明确的等待条件而非固定延时。

---

## 问题 8：`drawSceneWithUserView` 未接收和绘制 token 数据

**严重程度**：中（功能不完整）

`SandTable.drawSceneWithUserView`（第 325-332 行）只绘制了地图和视野，没有接收 token 列表来绘制场景中的棋子/墙壁/门等元素。

**问题代码**：
```typescript
async drawSceneWithUserView(proxyCfg?: ImageProxyConfig) {
    let oriMap = new Image();
    await WebUtil.loadImageByProxy(oriMap, this.scene.map.imageUrl, proxyCfg);
    this.scene.map.width  = oriMap.width ;
    this.scene.map.height = oriMap.height;
    await SandTableUtils.drawSceneWithUserView(this.scene, oriMap, this.observer);
}
```

**修改方案**：
```typescript
async drawSceneWithUserView(tokens: Array<IToken2D>, proxyCfg?: ImageProxyConfig) {
    let oriMap = new Image();
    await WebUtil.loadImageByProxy(oriMap, this.scene.map.imageUrl, proxyCfg);
    this.scene.map.width  = oriMap.width ;
    this.scene.map.height = oriMap.height;
    await SandTableUtils.drawSceneWithUserView(this.scene, oriMap, this.observer, tokens);
}
```

---

## 问题 9：`toRecord` 方法中 `color` 字段赋值不一致

**严重程度**：低（逻辑一致性）

`CircleToken.toRecord()`（第 69 行）将 `this.strokeStyle` 赋给 `color`，而非使用 `this.color`。虽然在构造时两者可能相同，但语义上应使用 `this.color`。

**问题代码**：
```typescript
toRecord(): ICircleTokenRec {
    return { "type": "Circle", "id": this.id, "x": this.c.x, "y": this.c.y, "radius": this.radius,
        "visiable": this.visiable, "blockView": this.blockView, "color": this.strokeStyle, "img": this.imgClip };
}
```

**修改方案**：
```typescript
toRecord(): ICircleTokenRec {
    return { "type": "Circle", "id": this.id, "x": this.c.x, "y": this.c.y, "radius": this.radius,
        "visible": this.visible, "blockView": this.blockView, "color": this.color, "img": this.imgClip };
}
```

---

## 问题 10：`RectangleToken.toRecord()` 中 `img` 字段硬编码

**严重程度**：中（数据丢失）

第 198 行 `toRecord()` 返回的 `img` 是硬编码值，而非序列化实际的 `this.imgClip`。

**问题代码**（第 193-199 行）：
```typescript
toRecord(): IRectangleTokenRec {
    return {
        "type": "Rectangle", "id": this.id, "x": this.x, "y": this.y,
        "width": this.width, "height": this.height, "visiable": this.visiable, 
        "blockView": this.blockView, "color": this.color,
        "img": { "imgKey": "icons", "sx": 0, "sy": 0, "width": 50, "height": 50 }
    };
}
```

**修改方案**：
```typescript
toRecord(): IRectangleTokenRec {
    return {
        "type": "Rectangle", "id": this.id, "x": this.x, "y": this.y,
        "width": this.width, "height": this.height, "visible": this.visible, 
        "blockView": this.blockView, "color": this.color,
        "img": this.imgClip
    };
}
```

---

## 问题 11：大量注释掉的死代码

**严重程度**：低（代码整洁）

文件中有多处被注释掉的代码块（第 338-348 行的颜色测试代码，第 370-391 行的 `drawToken` 方法，第 211-214 行的 `RectangleToken.draw` 中注释的 clip 逻辑）。

**修改方案**：删除所有注释掉的死代码，如需保留可通过 git 历史追溯。

---

## 问题 12：`drawScopeOfVisionOnDarkMap` 未先清空画布

**严重程度**：低（潜在的渲染 bug）

第 438 行直接 `drawImage` 但没有先 `clearRect`。如果 frame 的 canvas 被多次复用（如动态刷新），可能会叠加之前的残留内容。

**问题代码**（第 436-438 行）：
```typescript
export let drawScopeOfVisionOnDarkMap = async (frame: ICanvasFrame, ...) => {
    let width  = darkMapImage.width ;
    let height = darkMapImage.height;
    frame.ctx.drawImage(darkMapImage, 0, 0, width, height, 0, 0, width, height);
```

**修改方案**：
```typescript
export let drawScopeOfVisionOnDarkMap = async (frame: ICanvasFrame, ...) => {
    let width  = darkMapImage.width ;
    let height = darkMapImage.height;
    frame.ctx.clearRect(0, 0, width, height);
    frame.ctx.drawImage(darkMapImage, 0, 0, width, height, 0, 0, width, height);
```

---

## 问题汇总

| # | 问题 | 严重程度 | 类型 |
|---|------|----------|------|
| 1 | `visiable` 拼写错误 | 低 | 命名 |
| 2 | `viewRange` 类型签名不一致 | 中 | 类型安全 |
| 3 | `ISandTable` 空接口 | 低 | 设计 |
| 4 | `ScenceDataResp` 拼写错误 | 低 | 命名 |
| 5 | `fromRecord` 循环可简化 | 低 | 代码风格 |
| 6 | `fromRecord` 修改入参 | 中 | 副作用 |
| 7 | 硬编码 `sleep(1000)` | 中 | 性能/逻辑 |
| 8 | 未传递 token 数据到绘制 | 中 | 功能缺失 |
| 9 | `toRecord` color 字段用错属性 | 低 | 逻辑一致性 |
| 10 | `RectangleToken.toRecord` img 硬编码 | 中 | 数据丢失 |
| 11 | 大量注释死代码 | 低 | 整洁 |
| 12 | `drawScopeOfVisionOnDarkMap` 未清空画布 | 低 | 渲染 bug |

---



## 一、blog.ts 问题列表

### 1. XSS 安全漏洞 — innerHTML 直接拼接用户数据

**严重程度**: 高

`loadRecommandArticles` 和 `renderArticle` 中将服务端返回的数据直接拼接到 HTML 中，未经转义。如果服务端数据被污染，会导致 XSS 攻击。

**问题代码** (第 99-105 行):
```typescript
let html = "";
for (let i = 0; i < recommands.recommands.length; i++) {
    let t = recommands.recommands[i];
    html = html + `
        <li><div class="img-text-itm"><div class="item-thumbnail">
            <a href="${t.link}" target="_blank"><img class="img-hov" alt="" src="${t.thumbnail}" border="0"></a></div><div class="item-title">
            <a href="${t.link}">${t.title}</a></div></div><div style="clear: both;">
        </div></li>`;
}
```

**修改方案**:
```typescript
import { StrUtil } from './basic.js';

let html = "";
for (const t of recommands.recommands) {
    html += `
        <li><div class="img-text-itm"><div class="item-thumbnail">
            <a href="${StrUtil.escapeHtml(t.link)}" target="_blank"><img class="img-hov" alt="" src="${StrUtil.escapeHtml(t.thumbnail)}" border="0"></a></div><div class="item-title">
            <a href="${StrUtil.escapeHtml(t.link)}">${StrUtil.escapeHtml(t.title)}</a></div></div><div style="clear: both;">
        </div></li>`;
}
```

---

### 2. XSS 安全漏洞 — renderArticle 未转义标题和作者

**严重程度**: 高

**问题代码** (第 111-124 行):
```typescript
static renderArticle(atc: UserArticle) {
    let date = new Date();
    date.setTime(atc.time);
    let st = ShowdownUtils.makeHtml(atc.text);
    let html = `
        <div class="item">
            <div class="title">${atc.title}</div>
            <div class="metadata metadata-time">${date.toLocaleString()}</div>
            <div class="metadata metadata-auth"> by ${atc.auth}</div>
            <div class="body">${st}</div>
        </div>
        <div class="divider"><span></span></div>`;
    return html;
}
```

**修改方案**:
```typescript
static renderArticle(atc: UserArticle) {
    let date = new Date();
    date.setTime(atc.time);
    let st = ShowdownUtils.makeHtml(atc.text);
    let html = `
        <div class="item">
            <div class="title">${StrUtil.escapeHtml(atc.title)}</div>
            <div class="metadata metadata-time">${StrUtil.escapeHtml(date.toLocaleString())}</div>
            <div class="metadata metadata-auth"> by ${StrUtil.escapeHtml(atc.auth)}</div>
            <div class="body">${st}</div>
        </div>
        <div class="divider"><span></span></div>`;
    return html;
}
```

> 注: `atc.text` 经 ShowdownUtils.makeHtml 转换为 HTML 是有意为之（Markdown 渲染），这里不做转义。

---

### 3. XSS 安全漏洞 — loadUserInfo 未转义用户信息

**严重程度**: 高

**问题代码** (第 75-81 行):
```typescript
if (t1) t1.innerHTML = userInfo.user.userName   ;
if (t2) t2.alt       = userInfo.user.userName   ;
if (t2) t2.src       = userInfo.user.avatar     ;
if (t3) t3.innerHTML = userInfo.user.desc       ;
if (t4) t4.innerHTML = userInfo.user.joinTime   ;
if (t5) t5.innerHTML = userInfo.user.group      ;
if (t6) t6.href      = userInfo.user.homePageUrl;
```

**修改方案**:
```typescript
if (t1) t1.textContent = userInfo.user.userName;
if (t2) t2.alt         = userInfo.user.userName;
if (t2) t2.src         = userInfo.user.avatar;
if (t3) t3.textContent = userInfo.user.desc;
if (t4) t4.textContent = userInfo.user.joinTime;
if (t5) t5.textContent = userInfo.user.group;
if (t6) t6.href        = userInfo.user.homePageUrl;
```

> 使用 `textContent` 替代 `innerHTML`，浏览器会自动转义 HTML 实体，防止 XSS。

---

### 4. 拼写错误 — "Recommand" 应为 "Recommend"

**严重程度**: 低

**问题代码**:
```typescript
interface RecommandArticle { ... }
interface RecommandArticlesResp { ... }
static async loadRecommandArticles () { ... }
```

**修改方案**:
```typescript
interface RecommendArticle { ... }
interface RecommendArticlesResp { ... }
static async loadRecommendArticles () { ... }
```

> 需同步修改后端 API 路径 `loadRecommandArticles`，若后端无法同步修改则仅修改前端类型名。

---

### 5. API URL 硬编码

**严重程度**: 中

三处 API 调用都硬编码了 `http://www.jade-dungeon.cn:8088`，不利于环境切换（开发/测试/生产）。

**问题代码** (第 48、87、129 行):
```typescript
url: `http://www.jade-dungeon.cn:8088/api/blog/loadUserById?userId=${userId}`
url: "http://www.jade-dungeon.cn:8088/api/blog/loadRecommandArticles"
url: `http://www.jade-dungeon.cn:8088/api/blog/loadByUser?userId=${userId}&page=${currPage}`
```

**修改方案** — 利用已有的 `PageConfig.apiRoot`:
```typescript
// initWikiPage 中已有 cfg.apiRoot，可传递给各方法使用
static async loadUserInfo(apiRoot: string, userId: string) {
    // ...
    url: `${apiRoot}api/blog/loadUserById?userId=${encodeURIComponent(userId)}`
    // ...
}
```

---

### 6. URL 参数未编码

**严重程度**: 中

`userId` 直接拼接到 URL 中，若包含特殊字符会导致请求异常。

**问题代码**:
```typescript
url: `http://www.jade-dungeon.cn:8088/api/blog/loadUserById?userId=${userId}`
```

**修改方案**:
```typescript
url: `${apiRoot}api/blog/loadUserById?userId=${encodeURIComponent(userId)}`
```

---

### 7. 大量注释代码残留

**严重程度**: 低

第 152-173 行存在大段被注释掉的分页测试代码，影响可读性。

**修改方案**: 删除这些注释代码，相关历史可通过 git log 追溯。

---

### 8. 硬编码用户 ID 和页码

**严重程度**: 中

`initWikiPage` 方法中 `userId` 和 `currPage` 是硬编码的。

**问题代码** (第 215-216 行):
```typescript
let userId = "teo";
let currPage = 1;
```

**修改方案** — 从参数或 URL 中获取:
```typescript
static async initWikiPage(basePath: string, title: string, userId: string = "teo") {
    let currPage = 1;
    // 或从 URL search params 读取
    const params = new URLSearchParams(window.location.search);
    const pageUserId = params.get('userId') || userId;
    const pageCurrPage = parseInt(params.get('page') || '1', 10);
    // ...
}
```

---

### 9. 三次 API 请求串行等待，缺少错误处理

**严重程度**: 中

`loadUserInfo`、`loadRecommandArticles`、`loadUserArticles` 三者互不依赖，但串行执行浪费时间。同时没有 try-catch 处理网络错误。

**问题代码** (第 219-225 行):
```typescript
await BlogPage.loadUserInfo(userId);
await BlogPage.loadRecommandArticles();
await BlogPage.loadUserArticles(userId, currPage);
```

**修改方案**:
```typescript
try {
    await Promise.all([
        BlogPage.loadUserInfo(userId),
        BlogPage.loadRecommandArticles(),
        BlogPage.loadUserArticles(userId, currPage),
    ]);
} catch (e) {
    console.error("Blog page init failed:", e);
}
```

---

### 10. console.log 调试代码残留

**严重程度**: 低

第 56、93、140 行残留 `console.log` 调试输出。

**修改方案**: 删除或替换为条件式日志（如 `if (DEBUG) console.log(...)`）。

---

### 11. 导入了未使用的模块

**严重程度**: 低

**问题代码** (第 4 行):
```typescript
import { SimpleMap, SimpleStack, SimpleQueue } from './dataStructure.js'
```

实际只使用了 `SimpleMap`（第 127 行），`SimpleStack` 和 `SimpleQueue` 未使用。

**修改方案**:
```typescript
import { SimpleMap } from './dataStructure.js'
```

---


## 二、wiki.ts 问题列表

### 1. 导航栏配置与 blog.ts 完全重复

**严重程度**: 中（可维护性）

`wiki.ts` 第 13-29 行和 `blog.ts` 第 183-199 行的导航配置完全一致，违反 DRY 原则。修改导航结构需要同时改两个文件。

**问题代码** (wiki.ts 第 13-29 行):
```typescript
page.renderTopNav([
    { title: "Journal", link: "/" },
    { title: "Gallery", link: "/gallery.html" },
    { title: "Note", link: "//118.178.197.156/study/study/wiki_html" },
    // ... 完全重复
]);
```

**修改方案** — 抽取为共享常量:
```typescript
// 在 webHtmlPage.ts 或新建 siteConfig.ts 中
export const SITE_NAV_ITEMS: Array<NavTreeNode> = [
    { title: "Journal", link: "/" },
    { title: "Gallery", link: "/gallery.html" },
    { title: "Note", link: "//118.178.197.156/study/study/wiki_html" },
    {
        title: "About Me", subs: [
            { title: "Github", link: "//github.com/Jade-Shan/", isNewWin: true },
            { title: "", link: "" },
            { title: "Resume", link: "/resume.html" }]
    },
    {
        title: "Themes", subs: [
            { title: "hobbit", id: "switch-theme-hobbit"     , link: "#" },
            { title: "lo-fi" , id: "switch-theme-lo-fi"      , link: "#" },
            { title: "paper" , id: "switch-theme-paper-print", link: "#" }]
    }
];

// blog.ts / wiki.ts 中
page.renderTopNav(SITE_NAV_ITEMS);
```

---

### 2. 主题绑定配置重复

**严重程度**: 低（可维护性）

两个文件中主题配置数组完全相同。

**问题代码** (wiki.ts 第 67-71 行):
```typescript
let themes = [
    { elemSlt: "#switch-theme-hobbit"     , themeName: "hobbit"      },
    { elemSlt: "#switch-theme-lo-fi"      , themeName: "lo-fi"       },
    { elemSlt: "#switch-theme-paper-print", themeName: "paper-print" },
]
```

**修改方案** — 同导航栏，抽取为共享常量:
```typescript
export const SITE_THEMES = [
    { elemSlt: "#switch-theme-hobbit"     , themeName: "hobbit"      },
    { elemSlt: "#switch-theme-lo-fi"      , themeName: "lo-fi"       },
    { elemSlt: "#switch-theme-paper-print", themeName: "paper-print" },
];
```

---

### 3. IP 地址硬编码

**严重程度**: 中

导航栏中 Note 链接使用了裸 IP `//118.178.197.156/study/study/wiki_html`，不利于维护。

**修改方案**: 使用域名或配置常量代替。

---

### 4. `window.onresize` 赋值覆盖风险

**严重程度**: 低

**问题代码** (第 64 行):
```typescript
window.onresize = changeTocWithWindow;
```

直接赋值会覆盖其他代码注册的 resize 处理器。

**修改方案**:
```typescript
window.addEventListener('resize', changeTocWithWindow);
```

---

### 5. 导入了未使用的模块

**严重程度**: 低

**问题代码** (第 3 行):
```typescript
import { SyntaxHighlighterHelper, MathJaxHelper, BootStrapHelper, DataTableHelper } from './3rdLibTool.js';
```

所有导入都使用了，这条无问题。

> 实际确认: 四个都有使用（第 32、35、38-39、43-44 行），无问题。

---

## 三、共性问题汇总

| # | 问题类别 | 严重程度 | 涉及文件 |
|---|---------|---------|---------|
| 1 | XSS 漏洞（innerHTML 未转义） | 高 | blog.ts |
| 2 | API URL 硬编码 | 中 | blog.ts |
| 3 | URL 参数未编码 | 中 | blog.ts |
| 4 | 导航/主题配置重复 | 中 | blog.ts, wiki.ts |
| 5 | 串行请求可并行 | 中 | blog.ts |
| 6 | 硬编码用户 ID | 中 | blog.ts |
| 7 | window.onresize 覆盖 | 低 | wiki.ts |
| 8 | 拼写错误 Recommand | 低 | blog.ts |
| 9 | 注释代码残留 | 低 | blog.ts |
| 10 | console.log 残留 | 低 | blog.ts |
| 11 | 未使用的导入 | 低 | blog.ts |
| 12 | IP 硬编码 | 中 | wiki.ts |

---

## 四、优先修复建议

1. **立即修复**: XSS 漏洞（问题 1-3），安全风险最高
2. **短期优化**: API URL 提取为配置、URL 参数编码、并行请求
3. **中期重构**: 导航/主题配置抽取为共享模块，消除重复
4. **低优先级**: 拼写修正、注释清理、调试日志移除